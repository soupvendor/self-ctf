# ------------------------------------------------------------------
# internal-deployer service account
# Injected at build time so the deploy step can authenticate to CI.
# TODO(ops): move this to a build secret before we ship the image!!
# ------------------------------------------------------------------
DEPLOY_TOKEN=${FLAG_LAYERS}

# CI service account used by the deploy pipeline. The team hostname is assigned
# separately, so this artifact can be shared by every team and deployment.
CI_PORT=${GITEA_PORT}
CI_USER=${DEPLOY_USER}
CI_PASS=${DEPLOY_PASSWORD}
