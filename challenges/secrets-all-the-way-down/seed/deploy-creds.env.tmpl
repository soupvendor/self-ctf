# ------------------------------------------------------------------
# internal-deployer service account
# Injected at build time so the deploy step can authenticate to CI.
# TODO(ops): move this to a build secret before we ship the image!!
# ------------------------------------------------------------------
DEPLOY_TOKEN=${FLAG_LAYERS}

# CI service account used by the deploy pipeline. Also loaded by the seed
# container, so the account Gitea creates and the one players recover are the
# same values by construction.
CI_URL=http://${CHALLENGE_HOST}:${GITEA_PORT}
CI_USER=deploy-bot
CI_PASS=D3ploy-B0t-CI-2026!
