# self-ctf

Self hostable CTF challenges.

Docker Compose is the local and single-host path. Provider-specific deployment
implementations and their shared isolation contract live under [`deploy/`](deploy/README.md).
